<Window xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="RSAT Dashboard"
        WindowStyle="None"
        ResizeMode="NoResize"
        Width="1200"
        Height="750"
        Background="#0F172A">

    <Window.Resources>

        <!-- ============================================================
             PALETTE COLORI (Azure Blue Theme)
             ============================================================ -->
        <Color x:Key="BgDark">#0F172A</Color>
        <Color x:Key="BgPanel">#1E293B</Color>
        <Color x:Key="AzureBlue">#3B82F6</Color>
        <Color x:Key="AzureBlueLight">#60A5FA</Color>
        <Color x:Key="TextLight">#E2E8F0</Color>
        <Color x:Key="TextGray">#94A3B8</Color>

        <!-- ============================================================
             BRUSHES
             ============================================================ -->
        <SolidColorBrush x:Key="PanelBrush" Color="{StaticResource BgPanel}"/>
        <SolidColorBrush x:Key="BgBrush" Color="{StaticResource BgDark}"/>
        <SolidColorBrush x:Key="AccentBrush" Color="{StaticResource AzureBlue}"/>
        <SolidColorBrush x:Key="AccentLightBrush" Color="{StaticResource AzureBlueLight}"/>
        <SolidColorBrush x:Key="TextBrush" Color="{StaticResource TextLight}"/>
        <SolidColorBrush x:Key="TextGrayBrush" Color="{StaticResource TextGray}"/>

        <!-- ============================================================
             STYLE: HEADER TEXT
             ============================================================ -->
        <Style x:Key="HeaderText" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{StaticResource AccentLightBrush}"/>
            <Setter Property="FontSize" Value="26"/>
            <Setter Property="Margin" Value="0 0 0 20"/>
        </Style>

        <!-- ============================================================
             STYLE: SIDEBAR BUTTON
             ============================================================ -->
        <Style x:Key="SidebarBtn" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="Foreground" Value="{StaticResource TextGrayBrush}"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Width" Value="60"/>
            <Setter Property="Padding" Value="10"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Foreground" Value="{StaticResource AccentLightBrush}"/>
                    <Setter Property="Background" Value="#1E2535"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- ============================================================
             STYLE: AZURE CARD BUTTON (for View buttons)
             ============================================================ -->
        <Style x:Key="AzureCard" TargetType="Button">
            <Setter Property="Background" Value="#1E293B"/>
            <Setter Property="Foreground" Value="{StaticResource TextLight}"/>
            <Setter Property="BorderBrush" Value="{StaticResource AccentBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="15"/>
            <Setter Property="Margin" Value="10"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="10">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#24344D"/>
                </Trigger>
            </Style.Triggers>
        </Style>

    </Window.Resources>

    <!-- ============================================================
         MAIN GRID
         ============================================================ -->
    <Grid>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="80"/>   <!-- Sidebar -->
            <ColumnDefinition Width="*"/>    <!-- Main Content -->
        </Grid.ColumnDefinitions>

        <!-- ============================================================
             SIDEBAR
             ============================================================ -->
        <Border Grid.Column="0" Background="#111827">
            <StackPanel Margin="5" VerticalAlignment="Stretch">

                <!-- Spacer -->
                <Rectangle Height="20"/>

                <Button x:Name="BtnDashboard" Style="{StaticResource SidebarBtn}">
                    <StackPanel>
                        <TextBlock Text="🏠" FontSize="26" HorizontalAlignment="Center"/>
                        <TextBlock Text="Home" FontSize="10"/>
                    </StackPanel>
                </Button>

                <Button x:Name="BtnAD" Style="{StaticResource SidebarBtn}">
                    <StackPanel>
                        <TextBlock Text="👤" FontSize="26" HorizontalAlignment="Center"/>
                        <TextBlock Text="AD" FontSize="10"/>
                    </StackPanel>
                </Button>

                <Button x:Name="BtnDNS" Style="{StaticResource SidebarBtn}">
                    <StackPanel>
                        <TextBlock Text="🌐" FontSize="26" HorizontalAlignment="Center"/>
                        <TextBlock Text="DNS" FontSize="10"/>
                    </StackPanel>
                </Button>

                <Button x:Name="BtnCluster" Style="{StaticResource SidebarBtn}">
                    <StackPanel>
                        <TextBlock Text="🖧" FontSize="26" HorizontalAlignment="Center"/>
                        <TextBlock Text="Cluster" FontSize="10"/>
                    </StackPanel>
                </Button>

                <Button x:Name="BtnRunAs" Style="{StaticResource SidebarBtn}">
                    <StackPanel>
                        <TextBlock Text="🔐" FontSize="26" HorizontalAlignment="Center"/>
                        <TextBlock Text="RunAs" FontSize="10"/>
                    </StackPanel>
                </Button>

                <Button x:Name="BtnSearch" Style="{StaticResource SidebarBtn}">
                    <StackPanel>
                        <TextBlock Text="🔎" FontSize="26" HorizontalAlignment="Center"/>
                        <TextBlock Text="Search" FontSize="10"/>
                    </StackPanel>
                </Button>

                <StackPanel VerticalAlignment="Bottom">
                    <Rectangle Height="30"/>
                    <Button x:Name="CloseBtn" Style="{StaticResource SidebarBtn}">
                        <StackPanel>
                            <TextBlock Text="❌" FontSize="22" HorizontalAlignment="Center"/>
                            <TextBlock Text="Exit" FontSize="10"/>
                        </StackPanel>
                    </Button>
                </StackPanel>

            </StackPanel>
        </Border>

        <!-- ============================================================
             MAIN CONTENT AREA
             ============================================================ -->
        <Border Grid.Column="1"
                Background="{StaticResource BgBrush}"
                Padding="20">
            <ContentControl x:Name="MainContent"/>
        </Border>

    </Grid>

</Window>
